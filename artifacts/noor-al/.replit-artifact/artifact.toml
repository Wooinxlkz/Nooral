kind = "web"
previewPath = "/"
title = "NoorAl — Quran Platform"
version = "1.0.0"
id = "artifacts/noor-al"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22274

[services.development]
run = "pnpm --filter @workspace/noor-al run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/noor-al", "run", "build" ]
publicDir = "artifacts/noor-al/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22274"
BASE_PATH = "/"
